database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://postgresql-20240129120224.postgres.database.azure.com:5432/petclinic?sslmode=require}
spring.datasource.username=${POSTGRES_USER:pablo}
spring.datasource.password=${POSTGRES_PASS:olbap2002}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
